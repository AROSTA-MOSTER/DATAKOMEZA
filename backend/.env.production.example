# DATAKOMEZA Backend - Production Environment Configuration
# Copy this file to .env and update with production values

# Server Configuration
NODE_ENV=production
PORT=5000

# Database Configuration (Use managed PostgreSQL in production)
DB_HOST=your-production-db-host.com
DB_PORT=5432
DB_USER=datakomeza_prod
DB_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_IN_PRODUCTION
DB_NAME=datakomeza_production

# Security Keys (CRITICAL: Generate strong random keys for production)
# Generate JWT_SECRET: openssl rand -base64 64
JWT_SECRET=CHANGE_THIS_TO_STRONG_RANDOM_64_CHAR_STRING_IN_PRODUCTION

# Generate ENCRYPTION_KEY: openssl rand -base64 32 | cut -c1-32
ENCRYPTION_KEY=CHANGE_THIS_32_CHAR_KEY_PROD!!

# CORS Configuration
CORS_ORIGIN=https://your-production-domain.com

# MOSIP Mock Services Configuration
# For production, replace with actual MOSIP platform URLs
MOSIP_MDS_URL=https://mds.production.mosip.io
MOSIP_ABIS_URL=https://abis.production.mosip.io
MOSIP_SDK_URL=https://sdk.production.mosip.io
MOSIP_MV_URL=https://mv.production.mosip.io

# MOSIP API Configuration (Legacy - for backward compatibility)
MOSIP_API_URL=https://api.mosip.io/v1
MOSIP_API_KEY=your-production-mosip-api-key

# Logging Configuration
LOG_LEVEL=info

# Rate Limiting (Optional - for Redis-backed rate limiting)
# REDIS_URL=redis://your-redis-host:6379

# Monitoring (Optional)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# ELASTICSEARCH_URL=https://your-elasticsearch-host:9200

# Email Configuration (Optional - for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=noreply@datakomeza.org
# SMTP_PASSWORD=your-email-password

# SMS Configuration (Optional - for notifications)
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_PHONE_NUMBER=+1234567890

# Production Deployment Notes:
# 1. Never commit this file with real credentials
# 2. Use environment variables or secret management service
# 3. Rotate keys regularly
# 4. Enable HTTPS/SSL
# 5. Set up automated backups
# 6. Configure monitoring and alerts
# 7. Use managed database service
# 8. Enable database encryption at rest
# 9. Set up CDN for static assets
# 10. Configure load balancer for high availability
