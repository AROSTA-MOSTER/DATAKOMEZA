# Docker Environment Variables
# This file contains environment variables optimized for Docker deployment
# Copy this to .env in the backend directory when running with Docker

# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration (Docker service names)
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=1234567890
DB_NAME=datakomeza_dev

# JWT Configuration
JWT_SECRET=xK9mPqL3nW8vR2tY5uH7jF4bN6cM1aS0dG3hJ6kL9oP2qT5wE8rU1iO4yA7
JWT_EXPIRES_IN=7d

# Encryption
ENCRYPTION_KEY=zV3nQ8wL5pT2rY7uJ4fB9cN1mS0aH6

# MOSIP Mock Services (using host.docker.internal to access host services)
# If MOSIP services are not needed, these can be left as-is or removed
MOSIP_MDS_URL=http://host.docker.internal:4501
MOSIP_ABIS_URL=http://host.docker.internal:8081
MOSIP_SDK_URL=http://host.docker.internal:8080
MOSIP_MV_URL=http://host.docker.internal:8082
MOSIP_API_URL=https://api.mosip.io/v1
MOSIP_API_KEY=mock-api-key-for-development

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# File Upload
MAX_FILE_SIZE=5242880
UPLOAD_DIR=./uploads

# Logging
LOG_LEVEL=info
